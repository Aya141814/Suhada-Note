<div class="container mx-auto pt-3">
    <div class="row">
        <div class="col-lg-10 mx-auto">
            <!-- 検索フォーム -->
            <%# <form>
                <div class="flex mb-3">
                    <input class="form-control flex-grow border rounded-l-md p-2" placeholder="検索ワード" type="search" />
                    <div class="input-group-append">
                        <input type="submit" value="検索"
                            class="btn btn-primary bg-blue-500 text-white rounded-r-md p-2" />
                    </div>
                </div>
            </form>
        </div>
    </div> %>
            <!-- 掲示板一覧 -->
            <div class="row">
                <div class="col-12">
                    <div class="grid grid-cols-1 max-w-2xl mx-auto">
                        <% if @boards.present? %>
                        <%= render @boards %>
                        <% else %>
                        <div class="mb-3">掲示板がありません</div>
                        <% end %>
                    </div>
                </div>
            </div>
        </div>