<div class="container mx-auto px-4 py-8">
    <div class="max-w-3xl mx-auto bg-white rounded-lg shadow-lg p-6">
        <h2 class="text-2xl font-bold mb-6 text-center">獲得トロフィー</h2>

        <% if @trophies.present? %>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <% @trophies.each do |trophy| %>
            <div
                class="trophy-item bg-gradient-to-r from-amber-50 to-yellow-50 rounded-lg p-4 shadow-md border border-amber-200 hover:shadow-lg transition-all duration-300 flex items-center">
                <div class="trophy-icon mr-4 text-amber-500">
                    <% if trophy.icon.present? %>
                    <%= image_tag trophy.icon, alt: trophy.name, class: "w-12 h-12" %>
                    <% else %>
                    <span class="trophy-icon animate-trophy-bounce text-4xl inline-block">🏆</span>
                    <% end %>
                </div>
                <div class="trophy-details flex-1">
                    <h3 class="font-bold text-lg text-amber-800"><%= trophy.name %></h3>
                    <p class="text-gray-600 text-sm my-1"><%= trophy.description %></p>
                    <p class="trophy-date text-xs text-gray-500">
                        獲得日: <%= l current_user.user_trophies.find_by(trophy: trophy).achieved_at.to_date %>
                    </p>
                </div>
            </div>
            <% end %>
        </div>
        <% else %>
        <div class="text-center py-8 bg-gray-50 rounded-lg">
            <div class="trophy-icon text-4xl mb-4 opacity-50">🏆</div>
            <p class="text-gray-600">まだトロフィーを獲得していません。継続的に投稿してトロフィーを集めましょう！</p>
            <div class="mt-6">
                <%= link_to "新しい投稿を作成する", new_board_path, class: "btn btn-primary" %>
            </div>
        </div>
        <% end %>

        <div class="mt-8 text-center">
            <%= link_to "投稿一覧に戻る", boards_path, class: "btn btn-outline" %>
        </div>
    </div>
</div>